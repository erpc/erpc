# Data Models

Stateful systems and caches that require extra review.

## Caches + Storage

- Re-org aware cache (local + persistent).
- Scylla/DB integrations.
- Upstream response caching + TTLs.

## Consensus + Integrity

- Consensus policy inputs/outputs.
- Integrity checks across upstreams.

## Review Focus

- Correct invalidation on re-org.
- Idempotency + request de-dup.
- Backward compatibility for stored data.
